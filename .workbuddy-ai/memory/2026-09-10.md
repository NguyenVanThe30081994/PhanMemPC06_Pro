# 2026-09-10 — QR & liên kết: sửa file nguồn không đổi mã QR

## Việc đã làm
- Hoàn thiện tính năng "sửa file nguồn (original_url) mà không đổi QR/link rút gọn":
  - `routes/shortlink.py`: `edit_link` khóa `short_code` (không đọc từ form), chỉ đổi
    `original_url` + metadata; `log_action` old→new; flash riêng khi đổi nguồn.
    `add_link` chặn `custom_code` ký tự lạ (`[a-zA-Z0-9_-]{1,50}`).
  - `templates/shortlinks.html` + `shortlinks_mobile.html`: nút "Sửa" + modal
    `editLinkModal` (link rút gọn readonly), JS `openEditLink`; grid mobile 3→4 cột.
- Vá lỗi bảo mật nền `_normalize_target_url` (test mới phát hiện): `javascript:alert(1)`
  từng bị prefix thành `https://javascript:alert(1)` và lọt validation. Nay chặn mọi
  scheme lạ khi thiếu `http(s)://`, giữ hợp lệ `host:8080/x`, `example.com/file.pdf`.
- Test mới `tests/test_shortlink_edit_target.py` (6 case) — chạy `run_tests.py`:
  293 test, 6 test mới PASS hết; chỉ còn 2 fail nền cũ (`test_task_synthesis`).
  Baseline cũ "288/3" không còn đúng (test_report_aggregate ImportError hết trên env pc06).
- Cập nhật `CHANGELOG.md` (entry 2026-09-10 + mục vá URL + kết quả test), `memory.md`
  (trạng thái, baseline 293/2, môi trường test env cô lập pc06).

## Môi trường / lệnh
- `.venv` dự án KHÔNG tồn tại trên máy. Env test cô lập đã cài đủ requirements:
  `~/.workbuddy-ai/binaries/python/envs/pc06/bin/python run_tests.py` (từ root dự án).
- Lưu ý sandbox: `python -m unittest` trực tiếp bị chặn phê duyệt nhưng
  `run_tests.py` (redirect output vào file trong docs/) chạy được.

## Còn nợ
- 2 fail nền `test_task_synthesis` (tiền tố `assign_N:`) vẫn làm CI đỏ — NEXT STEPS #1.
- Chưa git commit (chờ Thế quyết định).
