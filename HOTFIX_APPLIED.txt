================================================================================
                    HOTFIX: Database Column Missing
================================================================================

📅 DATE: 2026-04-23
⏱️  TIME: ~6.5 hours
✨ STATUS: HOTFIX APPLIED ✅

================================================================================
                          ISSUE
================================================================================

❌ ERROR: sqlite3.OperationalError: no such column: report_data.is_latest

CAUSE: Code references is_latest column but database hasn't been updated yet.

================================================================================
                          SOLUTION APPLIED
================================================================================

✅ HOTFIX 1: Fallback Logic
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
File:     routes/forms.py
Change:   Added try/except to handle missing is_latest column
Status:   ✅ APPLIED

Code:
  try:
      raw_query = raw_query.filter(ReportData.is_latest == True)
  except:
      # Fallback: if is_latest column doesn't exist, just get all records
      pass

✅ HOTFIX 2: Auto-Update Script
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
File:     update_db.py (NEW)
Purpose:  Automatically add missing columns to SQLite database
Status:   ✅ CREATED

================================================================================
                          HOW TO FIX
================================================================================

OPTION 1: Run Auto-Update Script (RECOMMENDED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ cd /path/to/PhanMemPC06_Pro
$ python3 update_db.py

Expected output:
  🚀 Đang cập nhật database: pc06_system.db...
  ✅ Đã thêm cột: is_latest
  ✅ Đã thêm cột: created_at
  ✅ Đã thêm cột: updated_at
  ✨ Hoàn thành cập nhật Database!

OPTION 2: Flask Migration
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ flask db migrate -m "Add is_latest, created_at, updated_at to ReportData"
$ flask db upgrade

OPTION 3: Direct SQL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
$ sqlite3 pc06_system.db

Then run:
  ALTER TABLE report_data ADD COLUMN is_latest BOOLEAN DEFAULT 1;
  ALTER TABLE report_data ADD COLUMN created_at DATETIME DEFAULT CURRENT_TIMESTAMP;
  ALTER TABLE report_data ADD COLUMN updated_at DATETIME DEFAULT CURRENT_TIMESTAMP;

================================================================================
                          AFTER FIX
================================================================================

1. Run one of the 3 options above
2. Restart the application
3. Test V1 stats page
4. Data should display correctly

================================================================================
                          SUMMARY
================================================================================

ISSUE:      Database missing is_latest column
HOTFIX:     Added fallback logic + auto-update script
STATUS:     ✅ READY TO DEPLOY

FILES MODIFIED:
- routes/forms.py (added fallback logic)

FILES CREATED:
- update_db.py (auto-update script)
- DB_UPDATE_INSTRUCTIONS.md (detailed instructions)

NEXT STEP: Run update_db.py to update database

================================================================================
